import Product from "../data/products";

const links = {
    client: {
        home: "Banmano/",
        auth: "Banmano/auth",
        product: "/product/:productId",
    },
    panel: {
        panel: "Banmano/panel",
    },
}

export default links;