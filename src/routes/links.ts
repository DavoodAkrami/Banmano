import Product from "../data/products";

const links = {
    client: {
        home: "/",
        auth: "/auth",
        product: "/:productId",
        ai: "/ai-chat"
    },
    panel: {
        panel: "/panel",
    },
}

export default links;